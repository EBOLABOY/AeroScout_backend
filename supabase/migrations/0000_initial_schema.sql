-- Baseline migration
--
-- Generate the actual baseline from your live schema:
--   npx supabase db diff -f 0000_initial_schema
-- Then review and commit the generated SQL.

-- Placeholder file. Replace via `supabase db diff`.

